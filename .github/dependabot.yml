# Actions are pinned to commit SHAs in .github/workflows, so nothing floats to a
# new release on its own. Dependabot is what moves those pins forward.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
