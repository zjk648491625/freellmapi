[English](../../en/architecture/00-high-level-index.md) · **简体中文**

# 架构与内部实现

[← 返回 README](../../../README.zh-cn.md) · [文档索引](../README.md) · [深入解析域](OVERVIEW.md)

FreeLLMAPI 是一个自托管的、兼容 OpenAI 的网关，把约 34 家提供方的免费额度叠加在一个 `freellmapi-…` Bearer 令牌之后。一个 Express 代理暴露 `/v1/chat/completions`（以及 `/v1/responses`、`/v1/messages`、`/v1/completions`、`/v1/embeddings` 和 `/v1/models`）；每次请求时，路由器挑选一个状态健康、且未触及任何限流的最佳模型，在内存中解密其上游密钥，再把响应流式传回。

没有哪一家的免费额度足以单独支撑日常使用，所以路由器把目录当作一条汇聚的回退链来对待：通过 Thompson 采样的老虎机算法实时评估可靠性、速度、能力和余量，用持久化账本执行 RPM/RPD/TPM/TPD 与提供方级上限，并在墙钟预算内跨最多 20 次尝试进行故障转移。当链路顶端的模型耗尽当日额度时，端点会平滑降级到下一档健康模型，并在 UTC 午夜重置。

深入解析见 [OVERVIEW.md](OVERVIEW.md)。

- [工作原理](#工作原理)
- [尚不支持](#尚不支持)
- [局限性](#局限性)
- [服务条款审查](#服务条款审查)

## 工作原理

![一个请求进来，最好的免费模型出去 —— 带实时评分、冷却和额度追踪的回退链](../../../repo-assets/router-flow.png)

```
┌──────────────────┐   Bearer freellmapi-…   ┌─────────────────────────┐
│  OpenAI SDK /    │ ──────────────────────▶ │  Express proxy (:3001)  │
│  curl / any      │ ◀────────────────────── │  /v1/chat/completions   │
│  OpenAI client   │      streamed tokens    └────────────┬────────────┘
└──────────────────┘                                      │
                                                          ▼
                             ┌────────────────────────────────────────────────┐
                             │  Router                                        │
                             │   1. Pick highest-priority model that          │
                             │      (a) has a healthy key and                 │
                             │      (b) is under all its rate limits.         │
                             │   2. Decrypt key, call provider SDK.           │
                             │   3. On 429/5xx → cooldown + retry next model. │
                             └────────────────────────────────────────────────┘
                                          │
   ┌──────────────┬────────────┬──────────┴─────────┬─────────────┬──────────┐
   ▼              ▼            ▼                    ▼             ▼          ▼
 Google         Groq        Cerebras           OpenRouter        HF       …29 more
```

- **路由器**（`server/src/services/router.ts`）—— 为每次请求挑选模型。
- **限流账本**（`server/src/services/ratelimit.ts`）—— 由 SQLite 支撑的内存 RPM/RPD/TPM/TPD 计数器，遇到 429 时设置冷却。
- **提供方适配器**（`server/src/providers/*.ts`）—— 每个提供方一个文件，实现 `Provider` 基类的 `chatCompletion()` 与 `streamChatCompletion()`。
- **健康服务**（`server/src/services/health.ts`）—— 周期性探测，保持密钥状态最新。
- **仪表盘**（`client/`）—— 基于 React + Vite + shadcn/ui 的管理界面。
- **存储** —— SQLite（`better-sqlite3`），密钥使用 AES-256-GCM 信封加密。
- **工具调用救援**（`server/src/lib/tool-call-rescue.ts`）—— 把工具调用以纯文本而非结构化 JSON 输出的模型，其输出会被自动恢复为真正的 `tool_calls`；带工具的请求只会路由到确实支持工具的模型。
- **密钥导入与导出** —— 粘贴 `.env` 文件即可批量导入密钥（带预览与逐条选择），也可导出为 JSON、`.env` 或 CSV。

> 路由策略、老虎机评分、额度与冷却核算、流式传输、降级模式故障转移以及提供方目录，在深入解析中有实现层面的说明 —— 见 [OVERVIEW.md](OVERVIEW.md)。

## 尚不支持

范围是有意收窄的。如果某个功能既不在 README 的功能列表里，也不在下面，请默认它还不存在。

- **内容审核**（`/v1/moderations`）
- **`n > 1`**（单次请求返回多个补全）
- **按用户计费 / 多租户鉴权** —— 设计上就是单用户

非常欢迎添加上述任一功能的 PR。见[参与贡献](../../../README.zh-cn.md#参与贡献)。

## 局限性

叠加免费额度有实实在在的取舍，请对此保持诚实：

- **天花板是额度和可用性，不是模型级别。** 目录里确实有前沿级的条目 —— GPT-5.x、Grok 4.x、Kimi K3、DeepSeek V4 Pro、Gemini 3.x 都会出现在某家的免费额度里。你得不到的是对它们的*持续*访问：恰恰是这些条目日额度最小、排队最长、最可能在短时间内被下线或转为收费。请按容量和可用时间做预算，而不是按能力上限。
- **智能程度会随一天的推进而下降。** 排名最高的模型（通常是 Gemini 3.6 Flash、DeepSeek V4、Kimi K2.6）日额度最低。它们触顶后，路由器会顺着优先级链退到更小、更弱的模型。预期端点的有效智能在每天的后半段下降，然后在 UTC 午夜重置。
- **延迟波动很大。** Cerebras 和 Groq 极快，其他的则未必。你拿到的是当下可用的那一个。
- **免费额度可能不打招呼就变。** 提供方经常收紧、放宽或取消免费额度。发生时你会看到 429 或鉴权错误，直到目录更新送达 —— 实时源安装几天内就能收到修复，免费安装则跟随 30 天延迟。重新播种脚本在 `server/src/scripts/`。
- **根据定义，没有 SLA。** 需要可靠性就用有合同的付费提供方。
- **本地优先。** 没有多租户鉴权。只给自己用，不要暴露到公网。

## 服务条款审查

针对自托管、单用户、个人使用的场景，逐一重新审阅了各提供方的服务条款（2026 年 5 月）。摘要：

| 提供方 | 结论 | 说明 |
|---|---|---|
| Google Gemini | ⚠️ 谨慎 | 2026 年 3 月的条款把范围收窄为*“专业或商业用途，而非消费者使用”* —— 自托管的开发者代理仍可辩护，但这是新条款。 |
| Groq | ✅ 大概率可以 | GroqCloud 服务协议允许集成到客户应用中。 |
| Cerebras | ✅ 大概率可以 | 允许；明确禁止出售或转让 API 密钥。 |
| Mistral | ✅ 大概率可以 | API 允许用于个人或内部业务用途。 |
| OpenRouter | ✅ 大概率可以 | 2026 年 4 月的条款强化了禁止转售、禁止竞争性服务的条款；私有单用户代理仍然没问题。 |
| Cloudflare Workers AI | ⚠️ 模糊 | 没有反代理条款；受通用的自助订阅协议约束。 |
| NVIDIA NIM | ⚠️ 谨慎 | 试用条款 §1.2 / §1.4：*“仅供评估，不用于生产。”* 免费访问是持续的 40 RPM 限流（2025 年的积分制已停止），但仅供评估的范围依然有效。 |
| GitHub Models | ⚠️ 谨慎 | 免费层明确限定为*“实验”*和*“原型验证”*。 |
| Cohere | ❌ 避免 | 条款 §14 仍禁止*“个人、家庭或家用目的”*。 |
| 智谱（open.bigmodel.cn） | ✅ 大概率可以 | 平台文档中仍保留个人、非商业研究的豁免。 |
| Z.ai（api.z.ai） | ⚠️ 谨慎 | 新条目 —— 新加坡实体（与智谱中国不同）。§III.3(l) 的反流量重定向条款有可能被解读为针对代理；没有明确的个人使用豁免。 |
| Ollama Cloud | ✅ 大概率可以 | 新条目 —— 免费计划允许访问云端模型（1 个并发，5 小时会话上限）。未发现反代理或反转售条款。*（集成见 #14。）* |
| OVH AI Endpoints | ✅ 大概率可以 | 新条目（2026 年 6 月）—— 匿名访问有官方文档（每 IP 每模型 2 次/分钟）。OVH 保留引入词元或消耗上限的权利。 |
| AI Horde | ✅ 大概率可以 | 新条目（2026 年 6 月）—— 由 Haidra 非营利组织运营的免费社区公共资源；官方支持匿名使用（密钥 `0000000000`，队列优先级最低）。没有反代理或反转售条款。OpenAI 代理是试点，可能按用量受限。*（集成 #345。）* |

让大多数提供方满意的经验法则：**每家提供方一个账号**、**不转售**、**不与他人共享你的端点**、**不要把免费额度当付费生产后端来狂刷**。以上仅供参考，不构成法律意见 —— 请阅读各提供方的条款并自行判断。

自 2026 年 4 月的审查以来已移除：Moonshot 和 MiniMax 的直接集成已从目录中删除（Moonshot 转为仅付费；MiniMax 被 OpenRouter 的 `minimax/minimax-m2.5:free` 路由取代）。
